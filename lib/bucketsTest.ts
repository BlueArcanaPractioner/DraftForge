import { buckets } from '@/lib/buckets';

console.log('  commons  :', buckets.common.length);
console.log('  uncommons:', buckets.uncommon.length);
console.log('  rares    :', buckets.rare.length);
console.log('  mythics  :', buckets.mythic.length);
console.log('  lands    :', buckets.land.length);
